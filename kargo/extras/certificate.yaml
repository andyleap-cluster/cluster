apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kargo-tls
  namespace: kargo
spec:
  secretName: kargo-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - kargo.andyleap.dev
