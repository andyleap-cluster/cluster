apiVersion: v1
kind: Service
metadata:
  name: blog
  namespace: blog
  annotations:
    "git.andyleap.dev/singress-target": "andyleap.dev"
spec:
  selector:
    app: blog
  ports:
  - name: http
    port: 8080
    targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: blog
  namespace: blog
spec:
  revisionHistoryLimit: 0
  replicas: 1
  selector:
    matchLabels:
      app: blog
  template:
    metadata:
      labels:
        app: blog
    spec:
      containers:
      - image: ghcr.io/andyleap/blog@sha256:54cddd3c6299934e1f0d7c51a682ad6244eb66834f0697fd246e13095b014412
        name: blog
        ports:
          - containerPort: 8080
            name: http
        env:
        - name: S3_DOMAIN
          value: us-east-1.linodeobjects.com
        - name: S3_BUCKET
          value: andyleap-dev
        - name: S3_KEY
          valueFrom:
            secretKeyRef:
              name: s3-api-token
              key: key
        - name: S3_SECRET
          valueFrom:
            secretKeyRef:
              name: s3-api-token
              key: secret
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: blog-pg
              key: PG