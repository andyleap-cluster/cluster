apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-andyleap-dev
  namespace: traefik
spec:
  secretName: wildcard-andyleap-dev-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - andyleap.dev
    - "*.andyleap.dev"
